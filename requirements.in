# Top-level runtime dependencies. Versions pinned to what the project was verified against.
# Regenerate the lockfile with:  make lock   (or: python tasks.py lock)
--index-url https://pypi.org/simple

# Telegram
python-telegram-bot[job-queue]==21.6   # job-queue: conversation timeouts (2.2d)
python-dotenv==1.2.1

# AI / LLM
langchain==1.2.15
langchain-core==1.3.2
langchain-community==0.4.1
langchain-ollama==1.1.0
langchain-redis==0.2.5
langchain-anthropic        # lazily imported when USE_AI=anthropic
ollama==0.6.1
anthropic==0.97.0

# Google Calendar / Gmail
google-api-python-client==2.149.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.1

# Settings (Phase 0.4) — zenflow/settings.py
pydantic-settings==2.14.0

# Therapist web dashboard
fastapi==0.115.0
starlette==0.38.6
uvicorn==0.30.6
jinja2==3.1.6
python-multipart==0.0.26
itsdangerous==2.2.0
httpx==0.27.2
pydantic==2.12.5

# Redis (cache + relay sessions + LangChain history backend)
redis==5.1.1

# Crypto: Fernet encryption of stored Google OAuth tokens (web/gcal.py). Was missing before 0.2.
cryptography==46.0.5

# Images (Phase 4.3b): acupoint image ingestion — resize, thumbnails, EXIF stripping
pillow==12.3.0

# S3 media store (Phase 4.3c) — imported only when ZF_STORAGE_S3=1
boto3==1.43.96
